# תוכנית מוניטור נפרד לאילי

תאריך: 24.9.2026
מצב: תוכנית בלבד. שום קובץ במאגר לא שונה בהכנת המסמך הזה.

## ההחלטה ולמה

אילי מקבל מוניטור משלו. **לא מופע משותף.** אותו קוד, קובץ הגדרות לכל מופע, שני מאגרים נפרדים, שני מפתחות הצפנה נפרדים.

**הפרטיות כאן דו כיוונית.** איציק אמר במפורש שאינו מעוניין לראות את התוכן של אילי. אילי מקבל מוניטור ריק לשימוש עצמאי שלו. לכן לא מספיק שאילי לא יראה את החומר של איציק, אלא שגם ההפך נכון, וזו דרישה שמשנה את התשובה לשתי השאלות הפתוחות בסעיף 6 ומוסיפה את סעיפים 7 עד 10.

הסיבה טכנית ולא ארגונית. `gate.js` גוזר מפתח יחיד מתוך `gate-key.txt` (שורות 26 עד 31, `pbkdf2Sync` על הסיסמה והמלח), ו `build.js` אוטם איתו את כל המטען: כל אוסף בנפרד בשורה 499, והמטען המלא ב `head.json` בשורה 568. אין שכבת הרשאות בין אוסף לאוסף. מי שיודע את הקוד רואה הכל: את הצ׳אט, את הניקוזים, את התורים הרפואיים, את רשימת החסימות, את `payload.keys` עם נושאי ntfy וכתובת המייל. מופע משותף פירושו שאילי מקבל את התיק הרפואי של אביו. לכן שני מפתחות.

---

## 1. מפרט `instance.json`

הקובץ יושב בשורש המאגר. הוא **אינו** מכיל סודות: לא את מפתח השער, לא את טוקן ntfy בתשלום. הסודות נשארים היכן שהם היום, ב `gate-key.txt` וב `data/ntfy-token.txt`, שניהם ב gitignore.

### השדות

| שדה | טיפוס | מה זה | ברירת מחדל אם הקובץ חסר |
|---|---|---|---|
| `name` | מחרוזת | שם פנימי של המופע, משמש ללוגים ולשמות משימות | `"abaitzik"` |
| `owner` | מחרוזת | שם האדם, לתצוגה בלבד | `"איציק"` |
| `publicUrl` | מחרוזת | כתובת ה Pages המלאה, עם לוכסן בסוף | `"https://itcohen2-maker.github.io/abaitzik-monitor/"` |
| `basePath` | מחרוזת | הנתיב מתוך הכתובת, לניווט פנימי בדף | `"/abaitzik-monitor/"` |
| `origin` | מחרוזת | המקור לכותרות Origin ו Referer מול FormSubmit | `"https://itcohen2-maker.github.io"` |
| `ntfy.in` | מחרוזת | נושא הקליטה, מה שהדף שולח אליו והמאזין מאזין לו | `"abaitzik-in-95e62e86c34f4853"` |
| `ntfy.live` | מחרוזת | נושא הפעימה | `"abaitzik-in-95e62e86c34f4853-live"` |
| `ntfy.out` | מחרוזת | נושא ההתראות היוצאות אל הטלפון | `"abaitzik-cf9044bdcfa8"` |
| `mailbox` | מחרוזת | כתובת המייל שאליה FormSubmit מוסר | `"itcohen2@gmail.com"` |
| `dataDir` | מחרוזת | נתיב תיקיית הנתונים, יחסי לשורש המאגר | `"data"` |
| `taskPrefix` | מחרוזת | קידומת לשמות המשימות המתוזמנות בחלונות | `"AbaItzik"` |
| `pageTitle` | מחרוזת | כותרת הדף | `"אבא איציק בבנייה עצמית"` |
| `appTitle` | מחרוזת | הכותרת שנשמרת למסך הבית באייפון | `"המוניטור"` |
| `screens` | מערך או null | רשימת המסכים המופעלים. `null` פירושו הכל | `null` |
| `chromeProfile` | מחרוזת | הפרופיל שבדיקת התקינות פותחת | `"Profile 4"` |
| `memoryDir` | מחרוזת או null | תיקיית הזיכרון ש `backup.js` אורז. `null` פירושו בלי זיכרון. ראו סעיף 9 | `"C:/Users/User/.claude/projects/C--Users-User/memory"` |
| `driveFolder` | מחרוזת | תיעוד בלבד: לאן עולים הגיבויים. אצל אילי לא בחשבון itcohen2 | ריק |

הערה על `screens`: `null` הוא ברירת המחדל וחייב להישאר כזה, כי היום אין סינון בכלל. הערכים במערך הם מפתחות ה `PANES` שב `build.js` שורה 4776, למשל `"a"` לתשובות, `"q"` לרשתות, `"R"` לתזכורות. מסך שלא ברשימה נעלם מסרגל הניווט התחתון וממסך הבית, אבל **הקוד שלו נשאר בדף**. סינון בקוד עצמו הוא שינוי בסדר גודל אחר ולא נדרש לאילי.

### הקובץ של איציק

הקובץ הזה חייב לשחזר בדיוק את ההתנהגות הקיימת. אם הוא נמחק, כלום לא משתנה, כי כל ערך בו זהה לברירת המחדל.

```json
{
  "name": "abaitzik",
  "owner": "איציק",
  "publicUrl": "https://itcohen2-maker.github.io/abaitzik-monitor/",
  "basePath": "/abaitzik-monitor/",
  "origin": "https://itcohen2-maker.github.io",
  "ntfy": {
    "in": "abaitzik-in-95e62e86c34f4853",
    "live": "abaitzik-in-95e62e86c34f4853-live",
    "out": "abaitzik-cf9044bdcfa8"
  },
  "mailbox": "itcohen2@gmail.com",
  "dataDir": "data",
  "taskPrefix": "AbaItzik",
  "pageTitle": "אבא איציק בבנייה עצמית",
  "appTitle": "המוניטור",
  "screens": null,
  "chromeProfile": "Profile 4",
  "memoryDir": "C:/Users/User/.claude/projects/C--Users-User/memory",
  "driveFolder": ""
}
```

### הקובץ של אילי

הנושאים כאן הם דוגמה בלבד. הם **חייבים** להיווצר מחדש באקראי בשעת ההקמה, ראו סעיף 5.

```json
{
  "name": "ily",
  "owner": "אילי",
  "publicUrl": "https://ily-maker.github.io/ily-monitor/",
  "basePath": "/ily-monitor/",
  "origin": "https://ily-maker.github.io",
  "ntfy": {
    "in": "ily-in-3f7c1a9e04b2d856",
    "live": "ily-in-3f7c1a9e04b2d856-live",
    "out": "ily-out-8b41ce22f0a7"
  },
  "mailbox": "ily@example.com",
  "dataDir": "data",
  "taskPrefix": "Ily",
  "pageTitle": "המוניטור של אילי",
  "appTitle": "המוניטור",
  "screens": ["h", "a", "q", "r", "m", "e", "R", "T", "k"],
  "chromeProfile": "Profile 5",
  "memoryDir": null,
  "driveFolder": "הדרייב של אילי, לא itcohen2"
}
```

### הטוען

מודול חדש יחיד, `lib/instance.js`, בלי תלויות. הוא הדבר היחיד שקורא את הקובץ, וכל שאר הקבצים קוראים ממנו.

עקרון הברזל: **הטוען לעולם לא זורק.** קובץ חסר, קובץ פגום, שדה חסר, כולם נופלים לברירת המחדל שהיא הערך הנוכחי. מופע שנופל בגלל הגדרות הוא מוניטור שלא קולט הודעות, וזה בדיוק הכישלון שאסור.

מבנה מומלץ:

```js
// lib/instance.js
const DEFAULTS = { /* בדיוק הטבלה למעלה, הערכים של איציק */ };
function load() { /* קורא instance.json, ממזג עמוק מעל DEFAULTS, לא זורק לעולם */ }
module.exports = load();
```

---

## 2. רשימת השינויים, קובץ אחר קובץ

בכל שורה: הערך היום, ממה יקרא, ומה ברירת המחדל. **בכל המקרים ברירת המחדל שווה לערך הנוכחי, מילה במילה.**

### 2.1 `lib/instance.js` (חדש)

הקובץ היחיד שנוצר. מכיל את `DEFAULTS` ואת הטעינה.

### 2.2 `lib/notify-channels.js`

זה ערוץ היציאה. כל שינוי כאן מסוגל להשתיק את ההתראות של איציק בשקט.

| שורה | היום | יקרא מ | ברירת מחדל |
|---|---|---|---|
| 27 | `path.join(__dirname,'..','data','status')` | `inst.dataDir` | `'data'` |
| 52 | `const NTFY_TOPIC = 'abaitzik-cf9044bdcfa8'` | `inst.ntfy.out` | `'abaitzik-cf9044bdcfa8'` |
| 53 | `const MAILBOX = ['itcohen2','gmail.com'].join('@')` | `inst.mailbox` | `'itcohen2@gmail.com'` |
| 68 | `data/ntfy-token.txt` | `inst.dataDir` | `'data'` |
| 190 | `'Origin': 'https://itcohen2-maker.github.io'` | `inst.origin` | אותו ערך |
| 191 | `'Referer': 'https://itcohen2-maker.github.io/abaitzik-monitor/'` | `inst.publicUrl` | אותו ערך |
| 203 | `const NTFY_LIVE = 'abaitzik-in-95e62e86c34f4853-live'` | `inst.ntfy.live` | אותו ערך |

מה שלא נוגעים בו: מפת `CHANNELS` והמכסות בשורות 42 עד 46. המכסות הן של ntfy ושל FormSubmit, לא של המופע. שני מופעים על אותה מכונה חולקים כתובת IP אחת ולכן גם את אותה מכסה חינמית, וזה סיכון שרשום בסעיף הסיכונים.

### 2.3 `listen.js`

קו החיים. ההודעות של איציק נכנסות דרכו.

| שורה | היום | יקרא מ | ברירת מחדל |
|---|---|---|---|
| 36 | `const TOPIC = 'abaitzik-in-95e62e86c34f4853'` | `inst.ntfy.in` | אותו ערך |
| 37 | `const LIVE = TOPIC + '-live'` | `inst.ntfy.live` | `TOPIC + '-live'` |
| 38 עד 48 | `path.join(__dirname,'data',...)` | `inst.dataDir` | `'data'` |
| 79 | `docs/live.json` | ללא שינוי | ללא שינוי |

שימו לב לשורה 37: היום `LIVE` נגזר מ `TOPIC` בשרשור. אחרי השינוי הוא נקרא בנפרד, וברירת המחדל שלו חייבת להישאר הגזירה, אחרת מופע ישן בלי `instance.json` יאבד את הפעימה.

משתני הסביבה `ABAITZIK_CHAT_DIR` ו `ABAITZIK_DRAINS_DIR` בשורות 45 ו 47 **נשארים בדיוק כפי שהם** וגוברים על ההגדרות. הבדיקות משתמשות בהם.

### 2.4 `inbox.js`

| שורה | היום | יקרא מ | ברירת מחדל |
|---|---|---|---|
| 14 | `const TOPIC = 'abaitzik-in-95e62e86c34f4853'` | `inst.ntfy.in` | אותו ערך |
| 15, 16 | `data/ntfy-seen.json`, `data/inbox` | `inst.dataDir` | `'data'` |

### 2.5 `alarm.js`

| שורה | היום | יקרא מ | ברירת מחדל |
|---|---|---|---|
| 10 | קורא `data/ntfy-topic.txt` | `inst.dataDir` לנתיב, והקובץ נשאר המקור | `'data'` |
| 30 | `'Click': 'https://itcohen2-maker.github.io/abaitzik-monitor/'` | `inst.publicUrl` | אותו ערך |

### 2.6 `mailme.js`

| שורה | היום | יקרא מ | ברירת מחדל |
|---|---|---|---|
| 18 | `'/ajax/' + ['itcohen2','gmail.com'].join('@')` | `inst.mailbox` | `'itcohen2@gmail.com'` |
| 24 | `'Origin': 'https://itcohen2-maker.github.io'` | `inst.origin` | אותו ערך |
| 25 | `'Referer': '.../abaitzik-monitor/'` | `inst.publicUrl` | אותו ערך |

### 2.7 `healthcheck.js`

| שורה | היום | יקרא מ | ברירת מחדל |
|---|---|---|---|
| 25 | `const PROFILE = 'Profile 4'` | `inst.chromeProfile` | `'Profile 4'` |
| 28 | `const PUBLIC_URL = '...abaitzik-monitor/'` | `inst.publicUrl` | אותו ערך |
| 56 | `data/chat/chat` | `inst.dataDir` | `'data'` |
| 78, 147, 149, 156, 157, 161, 178, 182, 188 | שמות משימות `AbaItzikWorker`, `AbaItzikNightSleep`, `AbaItzikMorningWake`, `AbaItzikWave` | `inst.taskPrefix + 'Worker'` וכן הלאה | `'AbaItzik'` |

**כלל אדום כאן:** `chromeProfile` של איציק הוא `Profile 4` בלבד. אילי מקבל פרופיל אחר או מחשב אחר. אסור שההגדרות יאפשרו למופע של אילי להצביע על `Profile 4`.

### 2.8 `lib/store.js`

שורה 5, `DATA_DIR = path.join(__dirname,'..','data')`. יקרא מ `inst.dataDir`, ברירת מחדל `'data'`.

### 2.9 `build.js`

זה האזור הרגיש ביותר. השינויים כאן מינימליים בכוונה.

| שורה | היום | יקרא מ | ברירת מחדל |
|---|---|---|---|
| 548 | `data/keys.json` | `inst.dataDir` | `'data'` |
| 640 | `<meta name="apple-mobile-web-app-title" content="המוניטור">` | `inst.appTitle` | `"המוניטור"` |
| 644 | `<title>אבא איציק בבנייה עצמית</title>` | `inst.pageTitle` | אותו ערך |
| 10012 | `href="/abaitzik-monitor/"` | `inst.basePath` | `'/abaitzik-monitor/'` |
| 10020 | `location.href='/abaitzik-monitor/'` | `inst.basePath` | `'/abaitzik-monitor/'` |
| 50 | `path.join(__dirname,'data',dir,dir)` | `inst.dataDir` | `'data'` |
| 2660 עד 2800 | אריחי מסך הבית, `id="gMail"` וכו׳ | סינון לפי `inst.screens` | `null`, כלומר בלי סינון |
| 3395 עד 3420 | סרגל הניווט התחתון | סינון לפי `inst.screens` | `null` |

**מה לא נוגעים בו ב `build.js`, בשום שלב:**

- שורות 130 ו 153: `gate.enabled()` ובדיקת הכישלון הסגור. הבדיקה הזו היא מה שמונע פרסום בגלוי, והיא חייבת להישאר בדיוק כפי שהיא.
- שורות 172 עד 176: `renderPage` והחלפת `__GATE__`. הלוגיקה של "עם שער הדף ריק לגמרי" לא משתנה.
- שורה 499: אטימת האוספים לקבצים.
- שורות 565 עד 568: אטימת `head.json`.
- שורה 611: `now: gate.enabled() ? null : ...` ב `version.json`. הקובץ הזה נקרא לפני שיש מפתח ולכן אינו אטום, וההגנה היא שהוא לא נושא תוכן.
- שורות 3545 עד 3566 ו 9695: `secret()`, `MAILBOX()`, `NTFYIN()`, `LIVE_TOPIC()`. אלה כבר מוזנים מ `data/keys.json` דרך המטען האטום, כלומר הצד הזה **כבר חיצוני לקוד**. לא לגעת.

זו נקודה חשובה: הדף עצמו כבר מופרד מהזהות. `data/keys.json` הוא כבר קובץ הגדרות לכל דבר. הפער הוא בצד השרת, בסקריפטים שרצים על המכונה.

### 2.10 סקריפטי ההתקנה `setup-*.ps1`

`setup-listener.ps1` שורות 32 ו 35, `setup-night.ps1` שורות 18 ו 24, `setup-reminders.ps1` שורה 11, `setup-wave.ps1` שורות 28 ו 30, `setup-zinc-day.ps1` שורה 12, `setup-hospital-week.ps1` שורות 15 ו 23.

כולם מקודדים `AbaItzik` בשם המשימה. יקראו את `taskPrefix` מתוך `instance.json`, למשל:

```powershell
$inst = Get-Content -Raw instance.json -ErrorAction SilentlyContinue | ConvertFrom-Json
$prefix = if ($inst -and $inst.taskPrefix) { $inst.taskPrefix } else { 'AbaItzik' }
Register-ScheduledTask -TaskName "${prefix}Listener" ...
```

**סיכון ידוע:** שני מופעים על אותה מכונה חייבים קידומות שונות, אחרת `Register-ScheduledTask -Force` של אילי ידרוס את המשימה של איציק בשקט. לכן `taskPrefix` הוא שדה חובה מעשית, גם אם יש לו ברירת מחדל.

### 2.11 מה לא משנים בכלל

- `gate.js`. המפתח והמלח כבר קבצים חיצוניים בשורות 26 ו 27, אחד ל gitignore ואחד מפורסם בכוונה. אין מה להחצין.
- `.gitignore`. אם `dataDir` משתנה, הוא ישתנה גם שם, אבל לאילי יש מאגר משלו עם `.gitignore` משלו, ולכן עדיף להשאיר `dataDir` על `data` בשני המופעים ולא לגעת בקובץ הזה כלל. **זו ההמלצה.**
- `worker.js`, `push.js`, `remind.js`, `wave.js`, `zinc-day.js`, `backup.js`, `codex-bridge.js`. הם משתמשים ב `data` בנתיב יחסי לשורש המאגר, ומאגר נפרד פותר את זה מעצמו.

---

## 3. סדר ביצוע בטוח

הכלל לאורך כל הדרך: **כל שלב הוא קומיט אחד**, עם `npm test` ירוק לפניו ואחריו. שלב שנכשל חוזר אחורה ולא מתוקן קדימה.

### שלב 0, רשת ביטחון

לפני נגיעה אחת:

```
node backup.js
git rev-parse HEAD > .last-good
```

נבדק: הגיבוי קיים ונקרא. ההערה על `db-backup-to-drive` בזיכרון חלה כאן.

### שלב 1, המודול לבדו

יוצרים את `lib/instance.js`. **אף קובץ אחר לא משתנה.**

נבדק: `node -e "console.log(require('./lib/instance.js'))"` מדפיס בדיוק את ערכי איציק, גם כאשר `instance.json` לא קיים. `npm test` ירוק.

סימן אזהרה שמחייב עצירה: המודול זורק על קובץ חסר.

### שלב 2, `instance.json` של איציק

יוצרים את הקובץ עם הערכים מסעיף 1. הוא אמור להיות זהה לברירות המחדל.

נבדק: הדפסת המודול עם הקובץ ובלעדיו נותנת אותו פלט מילה במילה. השוואה אוטומטית עדיפה על עין.

### שלב 3, הקבצים השקטים

`lib/store.js`, `healthcheck.js`, `alarm.js`, `mailme.js`. אלה לא בקו החיים ולא בערוץ היציאה.

נבדק: `npm test` ירוק. `node healthcheck.js` נותן את אותו דוח כמו לפני. `node mailme.js` אינו מורץ באמת בשלב הזה, רק נקרא.

### שלב 4, `build.js`

הכותרות, ה `basePath`, ונתיב `keys.json`. **בלי סינון מסכים בשלב הזה.**

נבדק, לפי הסדר:
1. `MONITOR_NO_GATE=1 node build.js` ואז `npm test`.
2. בנייה רגילה עם המפתח, ואז **בדיקה ידנית חובה**: `docs/data/head.json` ו `docs/data/chat.json` נפתחים ואינם קריאים. חיפוש מחרוזת עברית מוכרת בתוכם חייב להחזיר אפס תוצאות.
3. `docs/index.html` אינו מכיל את המחרוזות `abaitzik-in-`, `abaitzik-cf`, `itcohen2@gmail`.
4. `git diff --stat docs/` לפני קומיט. אם מספר השורות בקבצי הנתונים קפץ בסדר גודל, זה סימן שהאטימה כבויה.

**סימן אזהרה שמחייב עצירה מיידית וחזרה אחורה:** אחת מהמחרוזות בבדיקה 3 נמצאת, או שקובץ ב `docs/data/` נפתח כ JSON תקין. במקרה כזה אסור לדחוף, כי הדחיפה מפרסמת הכל בגלוי על מאגר ציבורי.

### שלב 5, `lib/notify-channels.js`

ערוץ היציאה. שינוי קטן, בדיקה גדולה.

נבדק:
1. `npm test` ירוק.
2. `node -e "require('./lib/notify-channels.js').notify('בדיקה','בדיקת ערוץ')"` ואישור **בפועל** שההודעה הגיעה לטלפון, לא רק שהפונקציה החזירה `ok`.
3. `node -e "console.log(require('./lib/notify-channels.js').budget())"` מראה שהמונה עלה באחד, כלומר ההוצאה נספרה נכון.
4. `data/status/undelivered.json` לא גדל.

**סימן אזהרה:** ההודעה נכנסה לתור במקום לצאת, או שהמונה לא זז. שניהם אומרים שהנושא או הכתובת השתנו בטעות. חזרה אחורה.

### שלב 6, `listen.js` ו `inbox.js`

קו החיים. זה השלב היחיד שבו איציק עלול לא לקבל מענה בלי שאיש ישים לב.

הסדר בתוך השלב:
1. עוצרים את המאזין: `Stop-ScheduledTask -TaskName AbaItzikListener`.
2. משנים את `inbox.js` בלבד, מריצים `node inbox.js`, מוודאים שהוא קורא את אותו נושא ואינו כופל הודעות שכבר סומנו ב `data/ntfy-seen.json`.
3. משנים את `listen.js`.
4. מריצים בחזית, לא כמשימה: `node listen.js`.
5. שולחים הודעת בדיקה מהדף בטלפון ומוודאים שהיא נוחתת ב `data/inbox` תוך שניות, ושהפעימה נרשמת ב `data/status/live.json`.
6. רק אחרי שזה עבד, מחזירים את המשימה.

**סימני אזהרה שמחייבים עצירה:** הודעה שנשלחה לא הגיעה תוך דקה. הפעימה לא מתעדכנת. `ntfy-seen.json` התאפס, מה שיגרום לקליטה חוזרת של הודעות ישנות ולהצפת התיבה.

### שלב 7, סקריפטי ההתקנה

משנים את `setup-*.ps1` לקרוא `taskPrefix`.

נבדק: `Get-ScheduledTask | Where-Object TaskName -like 'AbaItzik*'` מחזיר בדיוק את אותה רשימה כמו לפני השינוי, עם אותם זמני הרצה הבאים.

### שלב 8, סינון מסכים

רק עכשיו, כשהכל יציב, מוסיפים את `screens`. אצל איציק הוא `null` ולכן שום דבר לא זז.

נבדק: הבנייה עם `screens: null` מייצרת `index.html` **זהה בתוכן** לבנייה לפני השינוי, פרט לחותמות הזמן ולמזהה הבנייה.

---

## 4. בדיקות

### מה קיים היום ומכסה חלק מהאזורים

- `tests/page.test.js`, 120KB, הבדיקה הכבדה. שורה 3 מדליקה `MONITOR_NO_GATE` כדי שהבדיקות יוכלו לקרוא את המטען. שורות 1397 עד 1411 בודקות ש `gate.seal` ו `gate.open` עובדים ושאותו טקסט נאטם פעמיים עם IV שונה. שורות 1427 עד 1430 בודקות את אחסון המפתח בדפדפן. שורות 1377 עד 1393 בודקות שסדר הערוצים בדף הוא ntfy ואז FormSubmit, ושהמייל יושב בתוך `catch`. שורה 691 בודקת קיום מזהי אלמנטים במסך.
- `tests/logic.test.js`, `tests/group.test.js`, `tests/drains.test.js`, `tests/autoclose.test.js`, `tests/unread-window.test.js`: כולן לוגיקה טהורה, לא נוגעות בזהות.
- `tests/truth-repro.js`: רץ בנפרד דרך `npm test`.

### הפער

**אין היום אף בדיקה על `lib/notify-channels.js`.** אין בדיקה על `listen.js`. אין בדיקה שמוודאת שהבנייה עם שער דלוק באמת אוטמת. אלה בדיוק שלושת האזורים הרגישים.

### הבדיקות שכדאי להוסיף

**`tests/instance.test.js`, הבדיקה החשובה ביותר.** היא נועדה לשאלה שאיציק שאל: שקובץ הגדרות חסר מחזיר בדיוק את ההתנהגות הנוכחית.

1. `instance.json` חסר לגמרי. הטוען מחזיר אובייקט שכל שדה בו שווה לערך שהיה מקודד קשיח לפני השינוי. הבדיקה מחזיקה את הערכים האלה כקבועים בתוכה, כדי שמישהו שישנה את ברירת המחדל יפיל אותה בכוונה.
2. `instance.json` הוא JSON פגום. הטוען לא זורק, ומחזיר את אותן ברירות מחדל.
3. `instance.json` עם שדה אחד בלבד. כל השאר ברירות מחדל, והשדה שסופק גובר.
4. `ntfy` קיים אבל בלי `live`. הגזירה `in + '-live'` נשמרת.

**`tests/channels.test.js`.** מזריקים `fetch` מזויף ובודקים שהכתובת שנקראה היא `https://ntfy.sh/abaitzik-cf9044bdcfa8` ושכתובת FormSubmit היא `itcohen2@gmail.com`, כשאין `instance.json`. זו בדיקת רגרסיה על ערוץ היציאה, והיא תופסת בדיוק את הטעות שתשתיק את איציק.

**`tests/listen-topic.test.js`.** בלי פתיחת רשת. מייצאים מ `listen.js` את `TOPIC` ואת `LIVE`, או קוראים את המודול עם `fetch` מזויף, ובודקים שהם `abaitzik-in-95e62e86c34f4853` ו `...-live` בהיעדר הגדרות. ייתכן שיידרש `module.exports` קטן ב `listen.js` לצורך זה, וזו תוספת מותרת.

**`tests/sealed.test.js`, ההגנה על האטימה.** מייצרים תיקיית נתונים זמנית עם מחרוזת סימן ייחודית, בונים **עם** שער, וקוראים את `docs/data/head.json`. הבדיקה דורשת שהסימן אינו מופיע, ושהקובץ אינו JSON תקין. זו הבדיקה היחידה שתתפוס פרסום בגלוי לפני שהוא קורה, וכדאי לכתוב אותה גם אם שום דבר אחר מהתוכנית לא מבוצע.

**`tests/screens.test.js`.** בונים עם `screens: null` ועם `screens` שמשמיט מסך, ומוודאים ששני הפלטים שווים כשהערך `null`.

---

## 5. רשימת ההקמה של אילי

הסדר כאן אינו המלצה. שינוי סדר בין צעד 2 לצעד 5 מפרסם את הנתונים של איציק.

### צעד 1, מאגר חדש וריק

מאגר חדש בשם `ily-monitor`. **לא פורק, לא שכפול של המאגר של איציק.** פורק גורר את ההיסטוריה, וההיסטוריה של המאגר הזה מכילה קומיטים מלפני שהשער הודלק.

```
git init ily-monitor
```

### צעד 2, `.gitignore` לפני הכל

זהו הצעד שאסור לדלג עליו. **מעתיקים את `.gitignore` מהמאגר של איציק כצעד הראשון, לפני שנוצרת תיקיית `data`, ולפני קומיט ראשון.**

הסיבה כתובה בקובץ עצמו: `/data/` חייב להיות מוחרג, וגם `gate-key.txt`, וגם `docs/files/lead-*`, וגם קבצי הגיבוי. אם `data/` נכנסת לקומיט ולו פעם אחת, היא נשארת בהיסטוריה לנצח, על מאגר שחייב להיות ציבורי כדי ש Pages יגיש אותו בחינם.

בדיקה חובה אחרי הצעד הזה, לפני כל קומיט:

```
git status --short
git check-ignore -v data/ gate-key.txt
```

`git status` חייב להיות ריק מכל דבר שתחת `data/`.

### צעד 3, הקוד

מעתיקים את קבצי הקוד בלבד: `build.js`, `listen.js`, `worker.js`, `gate.js`, `lib/`, `tests/`, `package.json`, סקריפטי ההתקנה. בלי `data/`, בלי `docs/`, בלי `backups/`, בלי `gate-key.txt`, בלי `gate-salt.txt`.

### צעד 4, מפתח ומלח חדשים

```
# מפתח חדש לגמרי, לא נגזרת של של איציק
node -e "console.log(String(Math.floor(Math.random()*1e6)).padStart(6,'0'))" > gate-key.txt
# את המלח gate.js ייצר לבד בבנייה הראשונה
```

**אסור להעתיק את `gate-key.txt` של איציק.** אם המפתחות שווים, שני המופעים אטומים באותו מפתח, ומי שיש לו את הקוד של אחד יכול לפתוח את הנתונים של השני, וכל ההפרדה בטלה.

`gate-salt.txt` נוצר אוטומטית בבנייה הראשונה (שורות 47 עד 52 ב `gate.js`). אסור להעתיק גם אותו.

### צעד 5, נושאי ntfy חדשים לגמרי

**זה הסעיף הקריטי השני.** נושא ntfy אינו מזהה, הוא סיסמה. מי שיודע את השם יכול גם להאזין לכל מה שמגיע לטלפון וגם לפרסם לתיבה, כפי שכתוב ב `data/keys.json` עצמו.

הנושאים של אילי חייבים להיות **אקראיים חדשים**, לא נגזרת של הקיימים. לא `abaitzik-in-...-ily`, לא אותו סיפרור עם ספרה שונה, לא החלפת קידומת. כל אחד מאלה מאפשר ניחוש מהאחר.

```
node -e "const c=require('crypto');console.log('ily-in-'+c.randomBytes(8).toString('hex'))"
node -e "const c=require('crypto');console.log('ily-out-'+c.randomBytes(6).toString('hex'))"
```

נושא הפעימה הוא נושא הקליטה עם הסיומת `-live`, כמו אצל איציק.

### צעד 6, `instance.json` ו `data/keys.json` של אילי

יוצרים את `instance.json` לפי הדוגמה בסעיף 1, עם הנושאים שנוצרו בצעד 5.
יוצרים `data/keys.json` חדש עם אותם נושאים ועם כתובת המייל של אילי. **אסור להעתיק את `keys.json` של איציק**, הוא מכיל גם את קטע `vaad` עם קישורים לגיליון ועם חובות של שכנים בשם.

### צעד 7, מבנה `data` ריק

יוצרים את תיקיות המשנה הריקות שהקוד מצפה להן: `data/chat/chat`, `data/status`, `data/inbox`, `data/reports`, ומה שהמסכים שנבחרו ב `screens` דורשים. **אף קובץ נתונים של איציק לא עובר.**

### צעד 8, GitHub Pages ובנייה ראשונה

מדליקים Pages על ענף `main` ותיקייה `docs`. מריצים `npm test` ואז `node build.js`. מוודאים ש `docs/data/head.json` אינו קריא.

### צעד 9, משימות מתוזמנות

אם המאזין של אילי ירוץ על אותה מכונה, `taskPrefix` חייב להיות `Ily`, ומריצים את `setup-listener.ps1`. כל משימה עוברת דרך `run-hidden.vbs`, אחרת קופץ חלון.

### מה אסור בשום אופן להעתיק, רשימה מרוכזת

| פריט | למה |
|---|---|
| `gate-key.txt` | מפתח משותף מבטל את כל ההפרדה |
| `gate-salt.txt` | נגזר יחד עם המפתח |
| כל תוכן `data/` | הצ׳אט, התורים הרפואיים, הניקוזים, אנשי הקשר |
| `data/keys.json` | מכיל את הנושאים של איציק ואת קטע ועד הבית |
| `data/ntfy-token.txt` | הטוקן בתשלום שלו |
| `data/no-reply.json` | שמות של אנשים אמיתיים |
| `docs/` בנוי | מכיל מטען אטום של איציק |
| `backups/`, `rivhit/` | גיבויים עם טלפונים ושמות בטקסט גלוי |
| נושאי ntfy, גם כווריאציה | הנושא הוא הסיסמה |
| היסטוריית git | קומיטים מלפני השער |

---

## 6. שתי שאלות פתוחות

### שאלה א, תחת איזה חשבון גיטהאב יהיה המאגר של אילי

**אם תחת החשבון של איציק** (`itcohen2-maker`): הכתובת תהיה מהצורה `https://itcohen2-maker.github.io/ily-monitor/`, כלומר **אותו מקור** כמו המוניטור של איציק. מה שמשתנה בתוכנית:
- `origin` זהה בשני המופעים, ולכן `localStorage` משותף לשני הדפים בדפדפן. `gateKey` השמור מדף אחד עלול להיקרא בדף השני. חובה לבדוק את קוד השער בדף ולוודא שהמפתח נשמר תחת מפתח אחסון שכולל את שם המופע, אחרת אילי בטלפון של איציק יקבל גישה בלי קוד. **זה הסיכון הגדול ביותר במסלול הזה.**
- ניהול קל יותר: חשבון אחד, אין הזמנות, הגיבוי דרך אותו מסלול.
- אילי תלוי באיציק לכל דחיפה.

**אם תחת החשבון של אילי**: מקור נפרד, `localStorage` נפרד, הבעיה למעלה נעלמת מעצמה. בתמורה:
- צריך לפתוח לו חשבון ולהפעיל Pages.
- אם איציק לא משתף פעולה על המכונה, `push.js` צריך אישורים של אילי.
- **זו ההמלצה שלי**, בעיקר בגלל הפרדת המקור.

### שאלה ב, מי יריץ את המאזין עבור אילי

**אם על המכונה של איציק**: שני תהליכי `listen.js` באותו מחשב. מה שמשתנה:
- `taskPrefix` חייב להיות שונה, אחרת `-Force` דורס.
- שני המופעים חולקים כתובת IP אחת, ולכן **את אותה מכסה חינמית של ntfy ושל FormSubmit**. המכסות ב `lib/notify-channels.js` שורות 42 עד 46 הן לכל מופע בנפרד, אבל השירות סופר לפי כתובת. שני מופעים במלוא הקצב יכולים להפיל את ההתראות של איציק. **פתרון: להוריד את המכסות של אילי בערך למחצית, או להשתמש בטוקן בתשלום נפרד.**
- `data/status/quota.json` נפרד לכל מאגר, ולכן אף אחד מהם לא רואה את ההוצאה של השני. זה בדיוק הכישלון של 13.9, בגרסה חדשה.
- מנגד: הגיבוי, בדיקת התקינות והתחזוקה במקום אחד.

**אם על מכונה של אילי**: המכסות נפרדות באמת, הפרופילים לא מתנגשים, אין סיכון לדריסת משימות. בתמורה, מכונה שצריכה להישאר דלוקה, ותחזוקה שאיציק לא רואה. אם אילי לא ישאיר מחשב דלוק, המוניטור שלו יהיה חצי מת, והקליטה תקרה רק כשמישהו יריץ `inbox.js` ידנית, כלומר בדיוק המצב שהיה לפני 13.9.

**המלצה:** להתחיל על המכונה של איציק עם מכסות מופחתות לאילי, ולעבור למכונה נפרדת רק אם המכסה נחנקת.

---

## 7. שתי רמות ההפרדה

הדרישה של איציק היא שלא יראה את התוכן של אילי. יש שתי דרכים להשיג את זה, והן אינן שוות ערך.

### 7.1 הפרדה מלאה

**ההגדרה:** המאגר תחת חשבון הגיטהאב של אילי, מפתח שער משלו, נושאי ntfy משלו, כתובת מייל משלו, והמאזין רץ על מכונה של אילי. איציק לא נוגע בנתונים בשום שלב.

**מה איציק יכול לראות:** את הקוד, כי הוא זהה בשני המאגרים. את כתובת ה Pages, שהיא ציבורית ממילא. שום דבר אחר.

**מה איציק לא יכול לראות:** את `data/` של אילי, כי היא יושבת על דיסק אחר. את התוכן של הדף, כי גם אם יפתח את הכתובת בדפדפן יקבל קליפה ריקה בלי המפתח, כמתואר בשורות 172 עד 176 ב `build.js`: עם שער דלוק הדף לא נושא נתונים כלל, הוא מושך את `head.json` ומפענח רק אחרי שהוקלד הקוד. את הגיבויים, כי `backup.js` של אילי כותב לתיקייה של אילי.

**מה נדרש:** חשבון גיטהאב לאילי, Pages דלוק אצלו, מכונה שנשארת דלוקה אצלו, והתקנת המשימות המתוזמנות שם.

**המחיר:** אם אילי לא ישאיר מחשב דלוק, המאזין לא ירוץ, וההודעות יחכו ב ntfy עד שמישהו יריץ `inbox.js` ידנית. זה בדיוק המצב שהיה לפני 13.9, כשששה מסרים קוליים של איציק חיכו תשעים ושבע דקות. מוניטור בלי מאזין הוא מוניטור חצי מת.

**זו הרמה היחידה שעונה על הדרישה של איציק במלואה.**

### 7.2 הפרדה חלקית

**ההגדרה:** שני המאגרים על המחשב של איציק, שני מפתחות נפרדים, שני מאזינים, קידומות משימות שונות.

**מה איציק לא יכול לראות:** את הדף של אילי דרך הדפדפן. בלי `gate-key.txt` של אילי הכתובת מחזירה קליפה ריקה, ו `docs/data/head.json` הוא בלוק AES ללא משמעות.

**מה איציק כן יכול לראות, וזו הבעיה:**

1. **`data/` הגולמית יושבת על הדיסק שלו, לא מוצפנת.** ההצפנה קורית רק בדרך אל `docs/`. תחת `ily-monitor/data/chat/chat/` כל הודעה היא קובץ JSON קריא בעורך טקסט. מי שיש לו גישה למחשב קורא הכל בלי שום קוד.
2. **`gate-key.txt` של אילי יושב על אותו דיסק.** בלי הצפנת דיסק ברמת המשתמש, הקובץ פתוח בפני איציק.
3. **`backup.js` סורק תיקייה שלמה**, ראו סעיף 9.
4. **הסשן שרץ במחשב**, כלומר אני, קורא ומכתיב את שתי התיבות. הפרדה שמסתמכת על כך שהסוכן לא יסתכל אינה הפרדה טכנית אלא מדיניות.

**מה נדרש כדי לצמצם את הסיכון, אם בכל זאת בוחרים ברמה הזו:**

- שני משתמשי חלונות נפרדים על המחשב, כל מאגר תחת פרופיל משתמש אחר, והמשימות המתוזמנות של אילי רצות תחת המשתמש שלו. זה מה שהופך את ההפרדה החלקית להפרדה אמיתית ברמת מערכת ההפעלה, ובלעדיו אין כאן הרבה.
- `backup.js` של אילי מכוון לתיקיית דרייב אחרת לגמרי, לא לחשבון `itcohen2`.
- הקבצים תחת `data/` של אילי מקבלים הרשאות שחוסמות קריאה מהמשתמש של איציק.
- `chromeProfile` שונה, ולא `Profile 4`.

**המסקנה שצריך לומר לאיציק בפה מלא:** הפרדה חלקית על משתמש חלונות אחד אינה מקיימת את מה שביקש. היא מונעת קריאה מקרית דרך הדפדפן, ולא יותר. אם הדרישה אמיתית ולא נימוסית, המסלול הוא הפרדה מלאה, או לכל הפחות משתמש חלונות נפרד.

---

## 8. מה הופך מופע לריק באמת

### 8.1 מה נבדק בפועל

בדקתי את זה ולא ניחשתי. העתקתי את `build.js`, את `gate.js` ואת `lib/` לתיקייה זמנית, בלי שום `data/` בכלל, והרצתי בנייה.

**התוצאה: הבנייה עוברת.** הפלט היה `built docs/index.html | 0 items, 0 pending, 0 replied, 0 contacts, 0 reports, 313KB page`, עם אזהרה אחת בלבד, `אין data/keys.json. הדף ייבנה בלי ערוצי שליחה`. נוצרו `index.html`, `404.html`, `robots.txt`, `version.json`, `videos.html`, וכל שלושה עשר קבצי האוספים תחת `docs/data/` ובהם `[]`.

חזרתי על הניסוי גם עם `data/` ריקה וגם בלי `data/` כלל. שתי הפעמים עברו.

**למה זה עובד:** `loadDocs` ב `build.js` שורות 49 עד 51 מחזירה `[]` כשהתיקייה חסרה, ו `store.load` ב `lib/store.js` שורות 19 עד 27 מחזירה את `EMPTY` על `ENOENT`. שתי נקודות הכניסה לנתונים מטפלות בהיעדר.

**המסקנה:** `build.js` **אינו** נופל על אוסף ריק. זה מוריד את הסיכון שהועלה בשאלה, אבל לא מבטל אותו, ראו 8.3.

### 8.2 מה בכל זאת כדאי ליצור

מבחינת הבנייה אין חובה. מבחינת הזרימה השוטפת כן, כי סקריפטים אחרים כותבים לתיקיות האלה ולא כולם יוצרים אותן לבד. הרשימה המינימלית:

| נתיב | מי כותב אליו | קריטיות |
|---|---|---|
| `data/chat/chat/` | `listen.js`, `worker.js` | חובה. בלעדיה אין צ׳אט |
| `data/status/` | `listen.js`, `lib/notify-channels.js`, `worker.js` | חובה. פעימה, מכסות ותור לא נשלחו |
| `data/inbox/` | `listen.js`, `inbox.js` | חובה. תיקיית הנחיתה של הצרופות |
| `data/reports/` | דוחות סבב | מומלץ |
| `data/commands/` | פקודות שאילי משאיר | מומלץ |
| `data/keys.json` | ידני | חובה, אחרת אין שליחה מהדף |
| `data/ntfy-topic.txt` | ידני | חובה עבור `alarm.js` |
| `data/state.json` | `lib/store.js` | לא נדרש. נוצר לבד |

התיקיות שלא יוצרים הן בדיוק המסכים שמכובים: `rivhit`, `pegasus`, `appts`, `reminders`, `food`, `drains`, `blocklist`, `special`, `replies`, `improve`, `choices`, `codex`, `contacts`, `tasks`.

### 8.3 מה כן קורה למסך ריק, וזה הסיכון שנשאר

`build.js` לא נופל, אבל מסך שנשאר דלוק בלי נתונים מציג **מסך ריק בלי הסבר**. הוא נראה כמו מסך שבור, לא כמו מסך שאין בו כלום. זה בדיוק הכישלון שאיציק כבר אמר עליו משהו: שלושת המונים במסך הבית שהראו אפס, אפס, אפס, והוא קרא להם מוקאפ שלא מחובר לכלום וביקש להוריד אותם. ההערה נמצאת ב `build.js` בשורות 2648 עד 2657.

**הפתרון:** לא לבנות מצב ריק חכם לכל מסך, אלא **לכבות את המסך** דרך `screens` ב `instance.json`. מסך שאין לו נתונים ואין לו מקור לא אמור להיות על המסך של אילי מלכתחילה.

**סיכון שנותר בתוקף:** אם ייכתב קוד חדש שמניח שאוסף אינו ריק, למשל `arr[0].x` בלי בדיקה, הוא ייפול אצל אילי ולא אצל איציק, ואף אחד לא ישים לב. לכן הבדיקה `tests/empty-instance.test.js` היא לא נחמדות: היא בונה מול `data` ריקה ודורשת יציאה בקוד אפס, והיא צריכה לרוץ ב `npm test` הרגיל. זו תוספת לרשימת הבדיקות בסעיף 4.

---

## 9. `backup.js`, וכן, זו בעיה אמיתית

### מה הקוד עושה היום

שלוש שורות קובעות את הכל:

- שורה 28: `const DATA = path.join(ROOT, 'data')`. יחסי לשורש המאגר.
- שורה 86: `for (const c of fs.readdirSync(DATA))`. **סריקה עיוורת של כל מה שיש בתיקייה.** אין רשימת אוספים מותרים, רק `NOT_A_COLLECTION` בשורה 35 שמחריגה `backup` ו `inbox`. כל תיקייה בצורת `data/<c>/<c>/` נכנסת לחבילה.
- שורה 30: `const MEM = 'C:/Users/User/.claude/projects/C--Users-User/memory'`. **נתיב מוחלט, מקודד קשיח, לזיכרון של איציק.**
- שורה 126: שם הקובץ הוא `abaitzik-backup-<תאריך>.json`.

### התשובה לשאלה

**כפי שהתוכנית נכתבה עד עכשיו, לא.** `backup.js` של אילי יקרא את `data/` של אילי, כי הנתיב יחסי לשורש המאגר ולאילי יש מאגר משלו. הוא לא יסחף את הנתונים של איציק, וגם לא להפך.

**אבל יש דליפה שכן קורית, והיא בכיוון ההפוך ממה שנשאל, כלומר מאיציק אל אילי:**

1. **שורה 30, `MEM`.** הנתיב מוחלט. `backup.js` שירוץ מתוך המאגר של אילי, על המחשב של איציק, **יארוז את כל קבצי הזיכרון של איציק** לתוך הגיבוי של אילי. הרשימה כוללת את `surgery-14sep2026.md`, את `itzik-hospital-week-sep2026.md`, את `vaad-habait-payments.md`, את `lolos-bookkeeping-reality.md` ועוד כמאה קבצים. זו הדליפה החמורה ביותר שמצאתי בכל הסקירה.
2. **שורה 120, `state.json` ו `ntfy-topic.txt`.** יחסיים, ולכן בסדר.

ובנוסף, הסיכון שהשאלה כיוונה אליו:

3. **יעד ההעלאה לדרייב.** `backup.js` עצמו רק כותב ל `backups/` המקומית. ההעלאה לדרייב היא צעד נפרד וידני, אל החשבון `itcohen2`. אם מי שמבצע את הצעד לא ישים לב, הגיבוי של אילי ינחת בדרייב של איציק, ואיציק יוכל לפתוח אותו. הקובץ אינו מוצפן.
4. **התנגשות שמות.** שני המאגרים מייצרים `abaitzik-backup-<אותו תאריך>.json`. אם שניהם מגיעים לאותה תיקייה, אחד דורס את השני, והשמירה של ארבעה עשר האחרונים בשורות 131 ו 132 מוחקת לפי אותה תבנית שם.

### מה צריך לשנות ב `backup.js`

| שורה | היום | השינוי | ברירת מחדל |
|---|---|---|---|
| 30 | `MEM` נתיב מוחלט לזיכרון של איציק | `inst.memoryDir`, שדה חדש ב `instance.json`. אצל אילי הערך `null`, והלולאה בשורות 109 עד 113 מדלגת | הנתיב הנוכחי מילה במילה |
| 126, 131 | `'abaitzik-backup-'` | `inst.name + '-backup-'` | `'abaitzik'`, כלומר אותו שם בדיוק |
| 31 | `OUT` בתוך המאגר | ללא שינוי, יחסי ולכן נפרד ממילא | ללא שינוי |
| 35 | `NOT_A_COLLECTION` | ללא שינוי | ללא שינוי |

`memoryDir` מצטרף לטבלת השדות בסעיף 1, עם ברירת מחדל `"C:/Users/User/.claude/projects/C--Users-User/memory"` ועם `null` אצל אילי.

**וצעד שאינו בקוד:** יעד הדרייב של אילי נקבע מראש, והוא **אינו** בחשבון `itcohen2`. אסור להעלות גיבוי של אילי לתיקיות של איציק, גם לא זמנית.

**סימן אזהרה בהרצה הראשונה:** הפלט של `node backup.js` אצל אילי מדפיס את מספר קבצי הזיכרון. אם הוא אינו אפס, שורה 30 לא טופלה. עוצרים ומוחקים את הקובץ שנוצר.

---

## 10. אילו מסכים לכבות אצל אילי

אצל אילי אין ריווחית, אין ועד בית, אין תורים רפואיים, אין תזכורות כדור ואין פגסוס.

### המיפוי

מפתחות ה `PANES` מ `build.js` שורה 4776, וכפתורי מסך הבית משורה 2660 ואילך:

| מסך | מפתח | כפתור | למה יורד |
|---|---|---|---|
| הנהלת חשבונות ולולוס | `o` | `gLolos` | אין עסק |
| ועד הבית וריווחית | `v`, `V`, `X` | `icVaad` | אין ועד בית. הנתונים מגיעים מ `keys.json` ומ `data/rivhit` |
| פגסוס | `x` | `gPegasus` | לא שלו |
| תורים עתידיים | `Q` | `gAppts` | רפואי |
| תזכורות | `R` | `gRemind` | תזכורות הכדור |
| אוכל ואבץ | `f` | לא על מסך הבית | רפואי |
| ניקוזים | `j` | הכפתור כבר ירד ב 20.9 | רפואי |
| לחסימה | `c` | `gBlock` | שייך לניהול הרשתות של איציק |
| בקשות ותגובות מיוחדות | `y`, `k` | הכפתורים כבר ירדו ב 23.9 | שייך לרשתות של איציק |
| יהודה והפי מיל | קישור ישיר | `gYehuda` | לקוח של איציק |
| ניטור רשתות | `q` | `gQueue` | שאלה לאיציק. הרשתות הן שלו |

מה שנשאר דלוק אצל אילי: בית, תשובות (`a`), צ׳אט ומייל (`e`), דוחות (`r`), משימות (`T`), פתקים (`t`), רעיונות (`i`), הקלטות (`g`), חיפוש והגדרות. זו בערך רשימת ה `screens` בדוגמה שבסעיף 1.

### איך `instance.json` שולט על זה

הסינון הוא **בזמן בנייה**, לא בזמן ריצה, כי בזמן ריצה הדף כבר ירד למכשיר.

שני מקומות ב `build.js`:

1. **מסך הבית**, שורות 2660 ואילך. אחרי הרכבת רשת האריחים, מסננים החוצה כל כפתור שה `pane` שלו אינו ברשימה. מיפוי מזהה כפתור למפתח מסך הוא קטן וקבוע, והוא צריך לשבת כטבלה אחת ליד `PANES` ולא להיות מפוזר.
2. **סרגל הניווט התחתון**, שורה 3395 ואילך. אותו סינון על `nH`, `nA`, `nQ`, `nL`, `nR`.

`screens: null` פירושו בלי סינון. זו ברירת המחדל, וזה מה שמשמר את המצב של איציק בדיוק.

**מה שהסינון אינו עושה:** הוא אינו מוציא את קוד המסך מהדף. `pV` ו `pRm` עדיין בנויים ב HTML, והמסלול `#j` עדיין פותח את הניקוזים אצל מי שמקליד אותו. זו החלטה מכוונת: הוצאת קוד מסכים מ `build.js` היא ניתוח בקובץ של חצי מיליון תווים, עם סיכון גבוה, בתמורה לחיסכון במשקל בלבד. אין שם נתונים של איציק, כי `data/` של אילי ריקה.

**המשמעות בפועל:** הסינון מסתיר, לא מוחק. הוא מספיק, כי מה שצריך להיות מוגן הוא הנתונים, והם מוגנים על ידי מאגר נפרד ומפתח נפרד.

### מה קורה למסך שמופעל בלי נתונים

מסך ריק בלי הסבר, כמתואר ב 8.3. לכן הכלל: **מסך נדלק אצל אילי רק אם יש מי שכותב אליו.** רשימת `screens` נגזרת מרשימת התיקיות שנוצרות תחת `data/` בסעיף 8.2, ולא להפך.

**בדיקה מומלצת שנובעת מכאן:** בדיקה שמאמתת ששני המערכים עקביים, כלומר שלכל מסך ב `screens` יש אוסף שהבנייה מכירה, ולכל אוסף שיש בו נתונים יש מסך דלוק. היא זולה, והיא תופסת את המצב שבו מישהו מוסיף מסך ושוכח את התיקייה, או הפוך.
